## How to clone whole project

Run the following command to initialize repo
```bash
    repo init -u https://github.com/yocto-is-easy/manifest 
```

And then sync repositories
```bash
    repo sync
```
